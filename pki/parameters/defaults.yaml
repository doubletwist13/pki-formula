# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  enabled: False
  pkg:
    name: pki
  rootgroup: root
  config: '/etc/pki'
  service:
    name: pki
  subcomponent:
    config: '/etc/pki-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
